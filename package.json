{
  "name": "main-boss-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "lefthook": "^1.11.0",
    "turbo": "^1.12.5"
  },
  "packageManager": "yarn@4.6.0",
  "dependencies": {
    "hbs": "^4.2.0"
  }
}
